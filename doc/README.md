# doc/ 文档索引

> 本目录是 lcdriv 的**正式文档（spec）**，面向实现与维护，是代码实现的唯一依据。
> 代码中不写注释——需要解释的内容一律落在此处。

## 职能分层

| 文档 | 职能 | 回答的问题 |
|---|---|---|
| `01-architecture.md` | 架构 | 为什么这样分层/为什么编译期配置 |
| `02/03/04-*.md` | 算法 spec | 每个类每个 public 函数**做什么**（可核对的数据） |
| `05-ut-design.md` | 测试设计 | 怎么验证 spec 被正确实现 |
| 根 `README.md` | 项目门面 | 这是什么、怎么构建/集成 |

## 文档清单

| 文档 | 对应物 | 内容 |
|---|---|---|
| `01-architecture.md` | 总览 | 定位与硬约束、三层架构、模板参数与特化策略、接口契约、支持矩阵与扩展路径、阶段路线 |
| `02-bus-spi.md` | `Bus<BusType::SPI>` | 算法文档：send / read 的具体功能 |
| `03-controller-ili9341.md` | `Controller<ControllerType::ILI9341, M, N>` | 算法文档：初始化序列、窗口/推帧、读 ID 等 |
| `04-lcddriver-spi-ili9341.md` | `LcdDriver<BusType::SPI, ControllerType::ILI9341, M, N>` | 算法文档：CS 事务、门面接口、双范式生命周期 |
| `05-ut-design.md` | — | UT 用例设计（gtest，FlowHub 风格）：HAL stub 方案、用例清单、CMake 接线 |

## 阅读顺序

1. 先读 `01-architecture.md`，建立整体框架。
2. 再自下而上读算法文档：`02-bus-spi` → `03-controller-ili9341` → `04-lcddriver-spi-ili9341`。
   （下层是上层的依赖：Controller 用 Bus，Driver 组合 Controller + Bus。）
3. 写测试/改协议数据前读 `05-ut-design.md`。

## 约定（算法文档模板）

- **每篇算法文档对应一个具体的类特化**（一个模板偏特化实例族 = 一篇）。
- 算法文档只描述该类**每个 public 函数的具体功能**，固定小节：
  - 定位、类签名；层间依赖（只用下层 public 接口，不列本类私有拆分，有则写）；
  - 每个 public 函数：**功能**（具体行为，寄存器/引脚层面效果）／**参数与返回值**／**前置/后置条件**／**副作用**／**边界与限制**；
  - 当前限制与后续计划。
- **不写伪代码**；**不体现内部实现**（私有函数拆了几个、内部状态等一律不出现）。
- **算法文档 = 可直接约束实现的 spec**：功能描述必须落到可核对的数据（命令码、参数字节、延时数值、编码规则、边界数值），不停留在"意图层"。
- 每篇 spec 头部标注**状态**与**数据来源**（实测驱动 / 数据手册）；协议数据以实测为准。
- 修改协议数据（如初始化序列、读 ID 时序）必须在文末**修订记录**登记，并注明需重新硬件实测。
- 新增一个类特化（新总线/新屏/新组合）→ 在 doc/ 加一篇对应算法文档，并在上表登记。
